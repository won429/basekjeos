# Nook 2.1.2 (빌드 212) — macOS 27 호환성과 단일 설치본

- macOS 27.0에서 실제 실행·빌드·회귀 검사를 수행했습니다.
- Finder, Spotlight, Dock이 이전 빌드를 다시 열지 않도록 `dist/`에는 검증된 ZIP만 생성합니다. 실행 앱의 정본은 `/Applications/Nook.app`입니다.
- 빈 `Nook 설정` 창을 버전, 권한 상태, 플레이어·YouTube Music·시스템 권한 바로가기를 보여주는 실제 설정 화면으로 교체했습니다.
- 설정 화면의 빠른 작업을 실제 앱 대리자에 직접 연결해 버튼이 간헐적으로 반응하지 않던 경로를 제거했습니다.
- macOS 27에서 바뀐 개인정보 보호 및 보안 확장 주소를 사용해 `손쉬운 사용 권한`과 `오디오 녹음 권한`이 각각 `기기 제어 및 데이터 접근`, `화면 및 시스템 오디오 녹음` 화면을 정확히 엽니다. macOS 26 이하에서는 기존 주소를 유지합니다.
- macOS 14에서 효력이 없어진 `activateIgnoringOtherApps`를 제거해 macOS 27의 앱 활성화 경로를 정리했습니다.
- 활성 SDK와 SwiftUI 매크로 플러그인을 함께 검증하는 SDK 선택기를 추가했습니다. 현재 이 Mac의 단독 Command Line Tools 27에 `SwiftUIMacros`가 없는 경우는 설치된 26.5 SDK로 안전하게 빌드하며, Xcode 27이 선택되면 27 SDK를 자동 사용합니다.
- 가사 회귀 검사가 현재 `ImmersivePlayerModel`과 `MediaRemoteClient` 의존성을 누락하지 않도록 테스트 빌드 범위를 교정했습니다.

검증: 앱 패키징·서명 검증, 핵심 회귀 검사, UI 검사 바이너리 빌드, macOS 27.0의 실제 플레이어 열기·닫기와 두 시스템 권한 바로가기를 확인했습니다.
