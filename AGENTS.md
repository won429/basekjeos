# 이 프로젝트의 작업 범위

사용자는 2026-09-07 이 폴더만 사용하는 별도 Codex 프로젝트를 요청했다.

- 작업 루트는 이 AGENTS.md가 있는 `Recovered-Nook-2.0-Project` 폴더다.
- 소스, 리소스, 테스트, 빌드 스크립트는 이 폴더 내부의 파일만 사용한다.
- 상위 또는 형제 폴더의 `NotchMusicSource`, `Verified-Nook-2.1-Project` 등으로 작업 위치를 바꾸거나 그 소스를 가져오지 않는다. 사용자가 별도로 요청한 경우에만 범위를 변경한다.
- 빌드는 이 폴더에서 `zsh scripts/build-app.sh`로 수행한다. 결과는 이 폴더의 `dist/`에서 확인한다.
- 버전 표시나 과거 복원 기록만으로 사용자가 제기한 화면·동작 차이가 해결됐다고 단정하지 않는다.
