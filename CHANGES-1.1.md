# Notch Music 1.1

1.0.6 소스 기준 최적화 버전입니다. 앱 표시 버전은 1.1, 빌드 번호는 98입니다. 기존 번들 ID(com.notchmusic.app)와 설정 키를 유지합니다. 제공 앱은 Apple Silicon(arm64)용입니다.

## 변경

- 보조 프로세스의 표준 출력과 오류 출력을 실행 중 동시에 읽습니다. 큰 앨범 이미지가 파이프를 채워 프로세스 종료와 읽기가 서로 기다리는 교착을 방지합니다.
- 일반 브리지 요청에 10초 시간 제한을 적용하고, 종료 신호에 응답하지 않으면 1초 후 정리합니다. 앱 종료 시 소유한 브리지 프로세스를 즉시 정리하므로 앱의 메인 실행 루프가 끝난 뒤 보조 프로세스가 남지 않도록 했습니다.
- 오디오 장치 생성·해제와 FFT 상태를 전용 직렬 큐에서 처리합니다. 느린 드라이버 응답이 UI 및 앱 종료 처리를 막지 않도록 분리하고, 중지·재시작 이전 캡처의 콜백을 제외합니다.
- AirPods 기기 조회를 백그라운드에서 처리합니다. 기존 1초 확인 주기와 연결 알림을 유지하며 중복 조회를 막습니다.
- 배터리·충전 상태는 macOS IOKit 변경 알림으로 감지합니다. 정상 등록 시 매초 조회를 없애고, 등록 실패 시 기존 조회 방식으로 대체합니다. 기존 잔량 경계와 알림 설정을 유지합니다.
- 같은 앨범 이미지의 Base64 변환과 전송, 적용된 이미지의 재디코딩을 줄입니다. 곡/소스 변경 및 이미지 재등장 시 필요한 이미지를 다시 전달합니다. 브리지의 기존 0.2초 확인 주기는 유지합니다.
- 변경되지 않은 재생 정보와 오디오 상태는 중복 발행하지 않습니다.
- 파형 분석의 기존 30Hz 상한, 2,048점 FFT, 9개 대역, 게인 및 스무딩 수식을 유지합니다. 버릴 버퍼는 모노 변환 전에 제외하고 FFT 작업 배열을 재사용합니다. Core Audio와 ScreenCaptureKit 경로는 같은 계산 함수를 사용합니다.
- 빌드 시 SDK 심볼릭 링크도 호환성 검사에 반영합니다. 배포 앱은 임시 폴더에서 서명하고, Finder 확장 메타데이터를 제외한 ZIP을 다시 풀어 서명을 검증합니다.

## 유지 확인

원본의 소스 및 리소스 파일을 삭제하지 않았습니다. NotchPlayerView, NotchPanelController, ImmersivePlayer, ImmersiveAppearance, PlayerSurfaceShape, NotchMusicApp을 포함한 UI·모션 구현은 원본과 바이트 단위로 동일합니다. AirPods 회전 프레임, 파형 표시 속도, 애니메이션 곡선·시간, 기능 메뉴, 언어와 설정 항목을 변경하지 않았습니다.

## 검증

- 핵심 검사 13종 통과: 브리지 프로세스/라인 버퍼, 배터리 경계/저전력 모드, 설정 마이그레이션, 앨범 색상, 노치 크기/윤곽/아이콘, 재생목록, 가사, 볼륨·밝기 키 판별.
- 브리지 회귀 검사: 표준 출력 및 오류 출력 각각 1MiB, 응답 없는 프로세스 타임아웃, 취소, 실행 실패.
- 원본과 FFT 결과 450회 비교 통과: 3개 샘플레이트, 5개 버퍼 크기, 6개 주파수와 반복 스무딩. 오차 1e-12 미만.
- JavaScript 브리지 검사 및 실제 Foundation NSData 캐시 검사 통과. 곡·정지·시간·이미지 전환 유지 확인. 고정 테스트 데이터의 전송량은 1,500,581 → 900,585바이트입니다. 이는 해당 테스트의 데이터 전송량이며 CPU·배터리 절감률이 아닙니다.
- 실제 테스트 창에서 8종 통과: 앨범 전환, 충전, 재생 전환, 확장/축소, 볼륨, 몰입형 화면, 정렬, 밝기 표시 및 가사 모션. 노치와 다이나믹 아일랜드를 확인했습니다.
- 경고 화면 축소 시 크기가 단조롭게 줄어드는지 보는 WarningMorph 검사는 1.0.6 원본과 1.1 모두 같은 항목에서 실패했습니다. 기존 모션 유지 요청에 따라 해당 UI 동작은 변경하지 않았습니다. 이를 최적화 후 통과한 검사로 계산하지 않았습니다.

실제 음악 앱·오디오 장치·AirPods를 연결한 장시간 CPU/전력/배터리 사용량 비교는 수행하지 않았습니다. 코드상의 교착 위험과 중복 작업을 줄였으며, 사용자의 모든 멈춤 상황이 해결됐다고 단정하거나 배터리 절감률을 제시하지 않습니다.

## 빌드 및 재검증

```sh
zsh scripts/build-app.sh
python3 scripts/run-core-checks.py
python3 scripts/run-ui-checks.py
python3 scripts/run-ui-checks.py --run
```

UI 검사는 실제 테스트 창을 엽니다. WarningMorph의 기존 실패 때문에 전체 UI 실행은 해당 항목에서 중단될 수 있습니다. 나머지는 다음 명령으로 실행할 수 있습니다.

```sh
python3 scripts/run-ui-checks.py --run ImmersivePlayer CompactAlignment MotionBrightness
```

FFT 원본 비교는 아래처럼 1.0.6 파일 경로를 넘깁니다.

```sh
python3 tests/verify-spectrum.py /path/to/1.0.6/Sources/NotchMusic/SystemAudioLevelMonitor.swift
```

검증 도구와 변경 소스는 함께 제공하며, 빌드 캐시와 테스트용 화면 캡처는 소스 ZIP에서 제외합니다. 서명은 기존 빌드와 같은 로컬 ad-hoc 방식이며 Developer ID 공증은 포함하지 않습니다.
